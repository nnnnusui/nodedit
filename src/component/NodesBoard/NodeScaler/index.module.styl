.NodeScaler
  content ''
  position absolute
  right 0
  bottom 0
  width 1em
  height 1em

  stroke var(--main-color)
  stroke-linecap round
  stroke-linejoin round
  stroke-width 4px

  cursor nw-resize
