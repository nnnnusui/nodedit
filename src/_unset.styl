*:not([contenteditable], html, body, rect)
  all unset
  display revert
  box-sizing border-box

html,
body
  all unset
  height 100%
  display flex
  flex-direction column
:root:not([x_x])
  a
    cursor pointer
  li
    list-style-type none

